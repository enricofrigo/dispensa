<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="16dp"
    android:gravity="center_vertical"
    android:background="?attr/selectableItemBackground">

    <ImageView
        android:id="@+id/imageView_drag_handle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_drag_handle"
        app:tint="?attr/colorControlNormal"
        android:contentDescription="@string/drag_handle_description" />

    <ImageView
        android:id="@+id/imageView_location_icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginStart="8dp"
        app:tint="?attr/colorControlNormal"
        android:visibility="gone"
        tools:src="@drawable/ic_fridge"
        android:importantForAccessibility="no" />

    <TextView
        android:id="@+id/textView_location_name_reorder"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="8dp"
        android:textAppearance="?attr/textAppearanceListItem"
        tools:text="Nome Location" />

    <!-- Potresti aggiungere un pulsante di modifica/eliminazione qui se lo desideri -->
    <ImageButton
        android:id="@+id/button_edit_location_item"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_edit_24"
        app:tint="?attr/colorControlNormal"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/edit_location"
        android:layout_marginStart="8dp"/>

    <ImageButton
        android:id="@+id/button_delete_location_item"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_delete_24"
        app:tint="?attr/colorError"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/delete_location"
        android:layout_marginStart="8dp"/>

    <ImageButton
        android:id="@+id/button_default_location"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_default_select"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/set_as_default"
        android:layout_marginStart="8dp"/>

</LinearLayout>
